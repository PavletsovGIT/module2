#ifndef CALC_H_
#define CALC_H_

#include <stdio.h>
#include <stdarg.h>

double Summ(int n, ...);
double Defference(int n, ...);
double Multipile(int n, ...);
double Divide(int n, ...);

#endif //CALC_H_
